# netflip-x 源码工程包（v5.2）

## 目录
- main.c      主程序源码（Win32 GUI，单文件）
- app.rc      资源脚本（图标 / UAC 清单 / 版本信息 VERSIONINFO）
- app.manifest  UAC 管理员权限清单
- icon.ico    程序图标
- gen_icon.py 图标生成脚本（可选）
- build.sh    Linux 交叉编译脚本（llvm-mingw）
- config.json 云端默认配置（与 GitHub 仓库同步）
- update.json 更新公告配置（与 GitHub 仓库同步）
- notice.json 纯公告配置（与 GitHub 仓库同步）

## 编译
### Linux（交叉编译 Windows exe）
需要 llvm-mingw 工具链，目录放在 toolchain/ 下，然后执行：
```bash
bash build.sh
```
产物：上网环境设置工具 v5.2.exe

### Windows（MinGW-w64）
```bash
windres --codepage=65001 app.rc -o app_res.o
x86_64-w64-mingw32-gcc -O2 main.c app_res.o -mwindows \
  -o 上网环境设置工具.exe \
  -lgdi32 -ladvapi32 -liphlpapi -lwininet -lurlmon -lshell32
```
注意：windres 必须加 --codepage=65001，否则版本信息中文会乱码。

## 在线功能（多源容错）
程序启动（互联网模式）会读取 GitHub 仓库的三个 JSON：
- config.json：参数设置窗「同步云端配置」读取
- update.json：有更新时弹更新公告（点确定自动下载）
- notice.json：纯公告（点确定仅关闭）

所有在线拉取统一走 fetch_json() 函数：按序尝试 5 个 jsDelivr 节点
（cdn / fastly / testingcf / gcore / jsdmirror，含国内镜像），
拉到后做内容校验（含关键字段才算成功，否则换源），并带总时限封顶，
后台线程执行、不阻塞界面。源列表在 main.c 的 fetch_json() 内，改仓库改这里。

说明：JSON 解析已兼容带引号与无引号的 true/false、数字。
